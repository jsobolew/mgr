project: rehersal Alexnet MNIST Task IL tr-t split v2
setup: taskIL
learning_rate: 0.001
architecture: SmallAlexNet
dataset: CIFAR10
rehearsal_dataset: stylegan-random
epochs: 5
contrastive_epochs: 0
batch_size_rehearsal: 128
batch_size: 128
pretraining: false
num_classes: 10
classes_per_task: 2
optimizer: # SGD Adam SGD_momentum
loss: CE
    # 'dead_leaves-mixed', 'dead_leaves-oriented', 'dead_leaves-squares',
    # 'dead_leaves-textures', 'stat-spectrum', 'stat-spectrum_color',
    # 'stat-spectrum_color_wmm', 'stat-wmm', 'stylegan-highfreq',
    # 'stylegan-random